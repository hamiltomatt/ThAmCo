@model ThAmCo.Events.Models.StaffingViewModel

@{
    ViewData["Title"] = "Remove Staff Booking";
}

<h2>Delete Booking</h2>

<h3>Are you sure you want to remove this?</h3>
<div>
    <h4>Booking for @Model.EventName</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.StaffName)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.StaffName)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="EventId" />
        <input type="submit" value="Remove" class="btn btn-default" /><p></p>
        <a asp-controller="Events" asp-action="Index">Back to List</a>
    </form>
</div>
